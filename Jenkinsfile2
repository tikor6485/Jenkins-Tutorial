pipeline {
    agent {lable "Linux"}
    options {
        buildDiscarder logRotator(artifactDaysTokeepStr: '', artifactNumToKeepS>
        disableConcrrentBuilds()
    } 
    stages {
        stage ('Hello'){
            steps{
                echo 'hello'
            }
        }
    }
}
